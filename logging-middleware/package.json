{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Logging middleware for URL shortener",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}